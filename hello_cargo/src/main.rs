fn main() {
    println!("Hello, world!");
}
// init:   cargo new hello_cargo
// build:  cargo build
// run:    ./target/debug/hello_cargo
// or:     cargo run
// (NOTE:  this compiles AND runs the code!)
// check:  cargo check
// (NOTE:  this check the code to make sure it compiles, but does not produce an executable)
// prod:   cargo build --release
// (NOTE:  compiles with optimizations into target/release)
// update: cargo update
// (NOTE:  update deps
// docs:   cargo docs --open
// (NOTE:  build dep docs locally and open in browser
// explain error: rustc --explain E0658
